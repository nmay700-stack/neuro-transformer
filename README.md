Title + badges — 🧠 Neuro-Transformer with shield.io badges for Python, PyTorch, MNE, Dataset, and Eval Scientific Motivation — the 50M epilepsy statistic, 1:500,000 neurologist ratio, and the paragraph explaining why Transformers model ictal synchrony Architecture — the ASCII pipeline diagram (Input → Linear Embedding → Spatial Positional Encoding → Encoder Layer 1 → Encoder Layer 2 → Global Avg Pool → [Ictal | Interictal]) with the "Why Transformers for EEG?" explanation below it Signal Preprocessing Pipeline — with the three steps (Notch filter, Bandpass filter, Z-score) each with code + rationale + the EEG bands table The Class Imbalance Problem — the 1,100:1 ratio explanation, the weighted loss code block, the WeightedRandomSampler code block Leave-One-Subject-Out Evaluation — the LOSO grid diagram, the data leakage explanation Interpretability: Attention Heatmaps — the ω[i,j] explanation and code snippet
